import 'package:flutter/material.dart';

const Duration microFeedback = Duration(milliseconds: 150);
const Duration stateChange = Duration(milliseconds: 200);
const Duration reveal = Duration(milliseconds: 300);
const Duration transition = Duration(milliseconds: 360);
const Duration hero = Duration(milliseconds: 400);
const Duration staggerItemDelay = Duration(milliseconds: 55);
const Duration cameraPulseDuration = Duration(milliseconds: 2000);
const Duration scanFadeDuration = Duration(milliseconds: 1500);
const Duration pageLoadDuration = Duration(milliseconds: 400);
const Duration successStampCircle = Duration(milliseconds: 600);
const Duration successStampCheck = Duration(milliseconds: 400);
const Duration filterSlide = Duration(milliseconds: 200);
const Duration cartBounce = Duration(milliseconds: 300);

const Curve easeOutCubic = Curves.easeOutCubic;
const Curve elasticOut = Curves.elasticOut;
